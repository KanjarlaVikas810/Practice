name: Workflow Practice 3

on: 
  push:
    branches:
      - main
  workflow_dispatch:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello"
    
  say-world:
    runs-on: ubuntu-latest
    needs: say-hello
    steps:
      - name: Say World
        run: echo "World"